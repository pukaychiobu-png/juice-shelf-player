<!doctype html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="theme-color" content="#08060f" />
  <title>Midnight Shelf — Juice WRLD</title>
  <style>
    :root {
      --ink: #08060f;
      --ink-2: #120d21;
      --text: #fffaff;
      --muted: #b6aebf;
      --line: rgba(255, 255, 255, 0.11);
      --accent: #ae7dff;
      --accent-2: #ff77b7;
      --accent-soft: rgba(174, 125, 255, 0.2);
    }

    * { box-sizing: border-box; }
    html { scroll-behavior: smooth; }

    body {
      min-width: 320px;
      min-height: 100svh;
      margin: 0;
      color: var(--text);
      overflow-x: hidden;
      background:
        radial-gradient(circle at 80% 7%, var(--accent-soft), transparent 29rem),
        radial-gradient(circle at 4% 75%, rgba(255, 100, 173, 0.09), transparent 31rem),
        var(--ink);
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      transition: background 800ms ease;
    }

    button,
    input,
    textarea { font: inherit; }
    button { cursor: pointer; }
    ::selection { color: #130d1d; background: var(--accent); }

    .aurora,
    .stars,
    .grain {
      position: fixed;
      inset: 0;
      pointer-events: none;
    }

    .aurora { z-index: -3; overflow: hidden; filter: blur(25px) saturate(1.2); }

    .aurora span {
      position: absolute;
      width: 130%;
      height: 35%;
      left: -15%;
      border-radius: 50%;
      mix-blend-mode: screen;
      background: linear-gradient(104deg, transparent 8%, var(--accent) 35%, transparent 70%), linear-gradient(80deg, transparent 30%, var(--accent-2) 65%, transparent 90%);
      opacity: 0.32;
      animation: aurora 16s ease-in-out infinite alternate;
    }

    .aurora span:nth-child(1) { top: -8%; transform: rotate(8deg); }
    .aurora span:nth-child(2) { top: 29%; opacity: 0.22; transform: rotate(-14deg); animation-delay: -6s; }
    .aurora span:nth-child(3) { top: 64%; opacity: 0.14; transform: rotate(10deg); animation-delay: -11s; }

    .stars { z-index: -2; overflow: hidden; }
    .star {
      position: absolute;
      width: var(--size);
      height: var(--size);
      left: var(--left);
      top: var(--top);
      border-radius: 50%;
      opacity: var(--opacity);
      background: #fff7ff;
      box-shadow: 0 0 13px var(--accent);
      animation: twinkle var(--duration) ease-in-out var(--delay) infinite alternate;
    }

    .shooting-star {
      position: absolute;
      width: var(--length);
      height: 1px;
      left: var(--left);
      top: var(--top);
      border-radius: 999px;
      opacity: 0;
      transform: rotate(var(--angle));
      transform-origin: left center;
      background: linear-gradient(90deg, transparent, #fff, var(--accent), transparent);
      box-shadow: 0 0 13px var(--accent);
      animation: shooting 1.6s cubic-bezier(.2,.75,.35,1) forwards;
    }

    .grain {
      z-index: 30;
      opacity: 0.065;
      mix-blend-mode: soft-light;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 180 180' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.88' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    }

    .storm-layer,
    .storm-flash {
      position: fixed;
      inset: 0;
      pointer-events: none;
    }

    .storm-layer {
      z-index: -1;
      overflow: hidden;
      opacity: 0;
      background:
        radial-gradient(ellipse at 22% 16%, rgba(85, 121, 178, .27), transparent 31%),
        radial-gradient(ellipse at 82% 74%, rgba(32, 74, 143, .34), transparent 38%),
        linear-gradient(160deg, #070b15 0%, #090d1a 48%, #05070d 100%);
      transition: opacity 900ms ease;
    }

    .storm-room .storm-layer { opacity: 1; }
    .storm-room { background: radial-gradient(ellipse at 50% -8%, rgba(69, 110, 190, .2), transparent 46rem), #050812; }
    .storm-room .aurora { opacity: .48; filter: blur(31px) saturate(.65) hue-rotate(172deg); }
    .storm-room .stars { opacity: .5; }

    .storm-layer::before,
    .storm-layer::after {
      content: "";
      position: absolute;
      width: 92vw;
      height: 31vh;
      border-radius: 50%;
      opacity: .55;
      filter: blur(33px);
      background: radial-gradient(ellipse, rgba(89, 116, 173, .44), rgba(12, 18, 34, .15) 48%, transparent 70%);
      animation: storm-clouds 18s ease-in-out infinite alternate;
    }

    .storm-layer::before { top: -12vh; left: -24vw; }
    .storm-layer::after { right: -27vw; bottom: 6vh; opacity: .32; animation-delay: -7s; }

    .storm-bolt {
      position: absolute;
      width: clamp(45px, 7vw, 93px);
      height: min(53vh, 490px);
      opacity: 0;
      background: linear-gradient(100deg, #f1fbff, #9cbfff 45%, #6278ff);
      clip-path: polygon(49% 0, 88% 0, 55% 34%, 89% 34%, 20% 100%, 39% 51%, 9% 51%);
      filter: drop-shadow(0 0 7px #effdff) drop-shadow(0 0 25px #7ca6ff);
      transform-origin: top center;
    }

    .storm-bolt.one { top: 3vh; left: 18%; transform: rotate(4deg); }
    .storm-bolt.two { top: -5vh; left: 58%; height: min(42vh, 400px); transform: rotate(-8deg); }
    .storm-bolt.three { top: 14vh; right: 8%; height: min(31vh, 310px); transform: rotate(14deg); }
    .storm-room .storm-layer.strike .storm-bolt { animation: lightning-bolt 540ms steps(2, end) both; }
    .storm-room .storm-layer.strike .storm-bolt.two { animation-delay: 78ms; }
    .storm-room .storm-layer.strike .storm-bolt.three { animation-delay: 155ms; }

    .storm-flash { z-index: 20; opacity: 0; mix-blend-mode: screen; background: radial-gradient(ellipse at 48% 17%, rgba(222, 245, 255, .9), rgba(125, 170, 255, .22) 29%, transparent 62%); }
    .storm-room .storm-flash.flash { animation: thunder-flash 600ms ease-out both; }

    .shell {
      position: relative;
      width: min(1320px, calc(100% - 36px));
      margin: 0 auto;
      padding: 34px 0 58px;
    }

    .topbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 20px;
      margin-bottom: 35px;
    }

    .top-actions { display: flex; align-items: center; gap: 10px; }

    .sync-state {
      display: inline-flex;
      align-items: center;
      gap: 7px;
      color: rgba(236, 225, 241, 0.58);
      font-size: 0.6rem;
      font-weight: 800;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    .sync-state::before {
      content: "";
      width: 6px;
      aspect-ratio: 1;
      border-radius: 50%;
      background: currentColor;
      box-shadow: 0 0 10px currentColor;
    }

    .sync-state.online { color: #8df3c6; }
    .sync-state.error { color: #ff9eaa; }

    .auth-button {
      padding: 9px 12px;
      border: 1px solid var(--line);
      border-radius: 999px;
      color: #fff;
      background: rgba(255,255,255,.05);
      font-size: .61rem;
      font-weight: 800;
      letter-spacing: .09em;
      text-transform: uppercase;
      transition: border-color 180ms ease, transform 180ms ease, background 180ms ease;
    }

    .auth-button:hover { border-color: var(--accent); background: color-mix(in srgb, var(--accent) 13%, transparent); transform: translateY(-2px); }

    .brand {
      display: flex;
      align-items: center;
      gap: 12px;
      color: #f5ebff;
      font-size: 0.7rem;
      font-weight: 800;
      letter-spacing: 0.18em;
      text-transform: uppercase;
    }

    .brand::before {
      content: "";
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: var(--accent);
      box-shadow: 0 0 17px var(--accent);
      animation: pulse 1.8s ease-in-out infinite;
    }

    .top-note {
      color: rgba(236, 225, 241, 0.51);
      font-size: 0.7rem;
      letter-spacing: 0.09em;
      text-transform: uppercase;
    }

    .intro {
      display: flex;
      justify-content: space-between;
      align-items: end;
      gap: 30px;
      margin-bottom: 37px;
    }

    h1 {
      max-width: 700px;
      margin: 0;
      font-size: clamp(3.4rem, 8vw, 7.5rem);
      font-weight: 820;
      letter-spacing: -0.1em;
      line-height: 0.82;
      text-shadow: 0 0 28px rgba(235, 219, 255, 0.18);
    }

    h1 span {
      display: block;
      margin-left: clamp(22px, 7vw, 92px);
      color: transparent;
      font-family: Georgia, "Times New Roman", serif;
      font-style: italic;
      font-weight: 400;
      letter-spacing: -0.09em;
      -webkit-text-stroke: 1px rgba(255, 247, 255, 0.72);
      text-shadow: 0 0 34px var(--accent);
    }

    .intro-copy {
      max-width: 270px;
      margin: 0 0 5px;
      color: var(--muted);
      font-size: 0.84rem;
      line-height: 1.6;
      text-align: right;
    }

    .app-grid {
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(340px, 0.82fr);
      gap: clamp(22px, 4vw, 58px);
      align-items: start;
    }

    .shelf-panel,
    .focus-panel {
      border: 1px solid var(--line);
      background: rgba(13, 8, 23, 0.59);
      box-shadow: 0 23px 75px rgba(0, 0, 0, 0.22), inset 0 1px rgba(255, 255, 255, 0.03);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
    }

    .shelf-panel {
      padding: clamp(18px, 3vw, 30px);
      border-radius: 31px;
    }

    .panel-heading {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 14px;
      margin-bottom: 22px;
    }

    .panel-heading h2,
    .focus-name {
      margin: 0;
      letter-spacing: -0.06em;
    }

    .panel-heading h2 { font-size: 1.34rem; }

    .count {
      color: var(--accent);
      font-size: 0.66rem;
      font-weight: 800;
      letter-spacing: 0.14em;
      text-transform: uppercase;
    }

    .search-wrap {
      position: relative;
      margin-bottom: 19px;
    }

    .search-wrap::before {
      content: "⌕";
      position: absolute;
      top: 50%;
      left: 15px;
      color: var(--muted);
      font-size: 1.25rem;
      transform: translateY(-52%);
    }

    .search-wrap input {
      width: 100%;
      padding: 13px 16px 13px 43px;
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 15px;
      outline: none;
      color: var(--text);
      background: rgba(255, 255, 255, 0.04);
      transition: border-color 180ms ease, background 180ms ease;
    }

    .search-wrap input:focus {
      border-color: color-mix(in srgb, var(--accent) 66%, white 16%);
      background: rgba(255, 255, 255, 0.07);
    }

    .song-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(165px, 1fr));
      gap: 11px;
    }

    .add-song-toggle {
      width: 100%;
      margin: 0 0 12px;
      padding: 12px 15px;
      border: 1px dashed color-mix(in srgb, var(--accent) 54%, white 18%);
      border-radius: 15px;
      color: var(--accent);
      background: color-mix(in srgb, var(--accent) 8%, transparent);
      font-size: .68rem;
      font-weight: 800;
      letter-spacing: .12em;
      text-align: left;
      text-transform: uppercase;
      transition: border-color 180ms ease, background 180ms ease, transform 180ms ease;
    }

    .add-song-toggle:hover:not(:disabled) { border-color: var(--accent); background: color-mix(in srgb, var(--accent) 15%, transparent); transform: translateY(-2px); }
    .add-song-toggle:disabled { cursor: not-allowed; opacity: .45; }
    .add-song-form { display: none; gap: 10px; margin: 0 0 16px; padding: 14px; border: 1px solid var(--line); border-radius: 18px; background: rgba(255,255,255,.035); }
    .add-song-form.open { display: grid; }
    .add-song-form input { width: 100%; min-width: 0; padding: 11px 12px; border: 1px solid rgba(255,255,255,.12); border-radius: 11px; outline: none; color: var(--text); background: rgba(0,0,0,.18); }
    .add-song-form input:focus { border-color: var(--accent); }
    .add-song-form .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
    .add-song-submit { justify-self: end; padding: 10px 13px; border: 0; border-radius: 999px; color: #160a1c; background: var(--accent); font-size: .66rem; font-weight: 850; letter-spacing: .1em; text-transform: uppercase; }
    .special-room-link{display:flex;align-items:center;justify-content:space-between;gap:20px;margin:22px 0;padding:20px 22px;border:1px solid rgba(255,104,84,.45);border-radius:20px;color:#fff;text-decoration:none;background:linear-gradient(115deg,rgba(255,37,66,.22),rgba(255,180,72,.11));box-shadow:0 0 45px rgba(255,30,62,.16)}.special-room-link b{font-size:1.1rem}.special-room-link span{color:#ffbd6d;font-size:.68rem;letter-spacing:.13em;text-transform:uppercase}

    .song-card {
      position: relative;
      min-height: 124px;
      padding: 16px;
      overflow: hidden;
      border: 1px solid rgba(255, 255, 255, 0.09);
      border-radius: 19px;
      color: #fff;
      background: rgba(255, 255, 255, 0.035);
      text-align: left;
      transition: transform 220ms ease, border-color 220ms ease, background 220ms ease, box-shadow 220ms ease;
    }

    .song-card::before {
      content: "";
      position: absolute;
      inset: auto -30px -50px auto;
      width: 110px;
      aspect-ratio: 1;
      border-radius: 50%;
      opacity: 0.53;
      background: radial-gradient(circle at 30% 30%, var(--card-accent), transparent 63%);
      filter: blur(5px);
      transition: transform 400ms ease;
    }

    .song-card:hover,
    .song-card.active {
      border-color: color-mix(in srgb, var(--card-accent) 58%, white 16%);
      background: color-mix(in srgb, var(--card-accent) 11%, transparent);
      box-shadow: 0 12px 34px color-mix(in srgb, var(--card-accent) 14%, transparent);
      transform: translateY(-3px);
    }

    .song-card:hover::before,
    .song-card.active::before { transform: scale(1.28); }

    .song-card.special {
      border-color: rgba(132, 177, 255, .48);
      background: linear-gradient(135deg, rgba(66, 106, 190, .18), rgba(8, 12, 27, .35));
      box-shadow: inset 0 1px rgba(203, 226, 255, .15), 0 12px 32px rgba(46, 93, 183, .15);
    }

    .song-card.special::after {
      content: "special room";
      position: absolute;
      z-index: 2;
      right: 12px;
      top: 12px;
      padding: 5px 7px;
      border: 1px solid rgba(188, 218, 255, .38);
      border-radius: 999px;
      color: #dbeeff;
      background: rgba(16, 28, 58, .5);
      font-size: .48rem;
      font-weight: 850;
      letter-spacing: .12em;
      text-transform: uppercase;
      backdrop-filter: blur(8px);
    }

    .storm-room .focus-panel { border-color: rgba(139, 180, 255, .33); box-shadow: 0 26px 82px rgba(0, 0, 0, .42), 0 0 40px rgba(71, 123, 255, .12); }
    .storm-room .cinematic-stage { background: radial-gradient(ellipse at 50% 41%, rgba(73, 120, 211, .18), transparent 37%), linear-gradient(150deg, #040812, #081020 58%, #040611); }
    .storm-room .cinema-vinyl { opacity: .56; box-shadow: 0 0 120px rgba(89, 143, 255, .34), inset 0 0 90px rgba(0,0,0,.78); }

    .song-card small,
    .song-card strong,
    .song-card span { position: relative; z-index: 1; display: block; }
    .song-card small { color: rgba(255, 255, 255, 0.48); font-size: 0.62rem; font-weight: 780; letter-spacing: 0.13em; text-transform: uppercase; }
    .song-card strong { max-width: 150px; margin: 11px 0 6px; font-size: clamp(1rem, 2.5vw, 1.25rem); letter-spacing: -0.055em; line-height: 1; }
    